import { ExportFileDirective } from './export-file.directive';

describe('ExportFileDirective', () => {
  it('should create an instance', () => {
    const directive = new ExportFileDirective();
    expect(directive).toBeTruthy();
  });
});
