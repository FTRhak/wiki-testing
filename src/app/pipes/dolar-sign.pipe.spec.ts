import { DolarSignPipe } from './dolar-sign.pipe';

describe('DolarSignPipe', () => {
  it('create an instance', () => {
    const pipe = new DolarSignPipe();
    expect(pipe).toBeTruthy();
  });
});
