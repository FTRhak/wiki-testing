import { QuickFilterPipe } from './quick-filter.pipe';

describe('QuickFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QuickFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
